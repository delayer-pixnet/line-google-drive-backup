# C# 軟體開發與產出標準規範

## 適用範圍

本規範適用於本 Repository。C# 專案依下列完整規範執行；TypeScript 與 Google Apps Script 專案亦須遵守分層、安全、命名、測試及繁體中文產出原則。

## 核心架構準則

1. 新增 C# Service 時，先定義 `I[Name]Service` 介面，再建立對應實作。
2. 商業邏輯封裝於 Service／BLL 層；UI 層不得直接撰寫 SQL、資料存取或複雜商業判斷。
3. 只修改需求必要的程式碼，不做無關重構。
4. 修改前確認既有流程與測試；修改後執行可用的建置、測試或靜態分析。

## 命名規範

1. Class、Struct、Enum、Delegate 與公開成員使用 `PascalCase`。
2. C# 介面使用大寫 `I` 加 `PascalCase`。
3. C# 私有欄位使用 `_camelCase`；區域變數與參數使用 `camelCase`。
4. 名稱須具明確語意並維持正式縮寫的一致大小寫。
5. 名稱包含 `_DT` 的欄位只能是 `DateTime` 或 `DateTime?`。
6. TypeScript 與 JavaScript 依各自生態慣例命名，並以一致性與語意清楚為優先。

## 資安與健壯性

1. SQL 必須參數化，且只能位於 DAL、Repository 或指定資料存取元件。
2. 檔案路徑須正規化、過濾非法字元並限制於允許根目錄。
3. 所有外部輸入須驗證型別、長度、範圍、格式與必要性。
4. 不得硬編碼或記錄密碼、Token、連線字串、Client Secret 或私密金鑰。
5. 不得吞掉例外；須保留可追蹤資訊並向使用者顯示安全訊息。

## 協作與靜態分析

1. 使用者要求檢查 C# 程式碼且環境有 `Code_Refactor_Tool` 時，必須使用該工具。
2. 若工具不存在，改用編譯器、分析器、測試或人工審查，且明確說明限制。
3. 回報須包含位置、風險、修正方式、相容性影響、驗證結果及異動檔案。

## 繁體中文排版規範

1. 文件、介面與錯誤訊息一律使用台灣繁體中文。
2. 中文與英文、半形數字之間保留 1 個半形空格；數字與單位之間亦同，百分比與度數除外。
3. 中文語句使用全形標點與直角引號；數字使用半形字元。
4. 專有名詞採官方拼寫，例如 GitHub、TypeScript、Next.js、JavaScript、SQL Server。
5. 使用「最佳化、使用者、功能、專案、流程、資訊、品質、啟用」等台灣慣用詞。

## 執行原則

1. 修改前閱讀根目錄至目前工作目錄適用的規範；較近且較嚴格者優先。
2. 不確定重要需求或規範衝突時，先說明衝突與預定處理方式。
3. 完成後列出異動檔案、建置／測試結果、尚未完成項目與已知風險。
